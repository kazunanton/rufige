<?php
/**
 * Пример конфигурации для save.php.
 *
 * Как использовать:
 *   1) На хостинге скопируйте этот файл в config.php (рядом с save.php).
 *   2) Замените admin_key на случайную строку 24+ символов.
 *      Сгенерировать можно командой:   openssl rand -base64 32
 *      Или в PowerShell:                [Convert]::ToBase64String((1..32|%{Get-Random -Max 256}))
 *   3) config.php НЕ коммитится в git (см. .gitignore).
 */

return [
    // Ключ администратора для входа в /admin/.
    // ОБЯЗАТЕЛЬНО заменить на уникальный секрет на проде.
    'admin_key' => 'CHANGE_ME',

    // Разрешённый Origin для CORS. Оставьте пустым, если анкета и save.php
    // на одном домене. Пример: 'https://survey.example.ru'.
    'allowed_origin' => '',

    // Максимальный размер тела POST-запроса, байт.
    'max_body_bytes' => 200000,

    // Минимальное допустимое время заполнения анкеты, мс. Ответы, присланные
    // быстрее, считаются ботами и тихо отбрасываются.
    'min_fill_ms' => 30000,

    // Лимит POST-запросов с одного IP за 60 секунд.
    'rate_limit_per_min' => 10,
];
